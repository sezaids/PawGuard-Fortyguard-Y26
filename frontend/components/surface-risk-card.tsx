export type SurfaceRisk = { score: number; level: "Low" | "Moderate" | "High" | "Very High"; surface: string; reason: string; main_factors: { factor: string; points: number; detail: string }[]; safer_alternatives: string[]; disclaimer: string };
const tone = { Low: "bg-emerald-500", Moderate: "bg-amber-400", High: "bg-orange-500", "Very High": "bg-red-600" };

export function SurfaceRiskCard({ risk }: { risk: SurfaceRisk }) { return <article className="rounded-3xl bg-white p-6 shadow-card"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-semibold text-moss">ESTIMATED PAW-SURFACE RISK</p><h2 className="mt-1 text-2xl font-bold">{risk.level}</h2></div><span className={`grid h-14 w-14 place-items-center rounded-2xl ${tone[risk.level]} text-lg font-bold text-white`}>{risk.score}</span></div><p className="mt-5 text-sm leading-6 text-ink/75">{risk.reason}</p><div className="mt-5"><p className="text-xs font-semibold uppercase tracking-wide text-moss">Safer alternatives</p><ul className="mt-3 space-y-2 text-sm text-ink/70">{risk.safer_alternatives.map(item => <li key={item}>• {item}</li>)}</ul></div><p className="mt-5 rounded-xl bg-sand p-3 text-xs leading-5 text-ink/55">{risk.disclaimer}</p></article>; }
