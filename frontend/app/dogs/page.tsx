"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { AuthGate } from "../../components/auth-gate";
import { Sidebar } from "../../components/sidebar";
import { Dog, request } from "../../lib/api";
export default function DogsPage() { const [dogs, setDogs] = useState<Dog[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { request<Dog[]>("/dogs/").then(setDogs).finally(() => setLoading(false)); }, []); return <AuthGate><main className="min-h-screen bg-sand md:pl-72"><Sidebar /><div className="mx-auto max-w-6xl px-5 pb-12 pt-20 sm:px-8 md:pt-10"><header className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold text-moss">MY DOGS</p><h1 className="mt-1 text-3xl font-bold">Your pack</h1><p className="mt-2 text-ink/60">Profiles help PawGuard tailor future safety guidance.</p></div><Link href="/dogs/new" className="rounded-xl bg-ink px-5 py-3 text-sm font-semibold text-white">+ Add a dog</Link></header>{loading ? <p className="text-ink/60">Loading profiles…</p> : dogs.length === 0 ? <section className="rounded-3xl border border-dashed border-moss/25 bg-white p-10 text-center shadow-card"><span className="text-4xl">🐾</span><h2 className="mt-4 text-xl font-bold">Your pack is waiting</h2><p className="mx-auto mt-2 max-w-md text-sm text-ink/60">Add your first dog to start building their PawGuard profile.</p><Link href="/dogs/new" className="mt-6 inline-block rounded-xl bg-moss px-5 py-3 text-sm font-semibold text-white">Add your first dog</Link></section> : <section className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{dogs.map(dog => <Link href={`/dogs/${dog.id}`} key={dog.id} className="rounded-3xl bg-white p-6 shadow-card transition hover:-translate-y-1"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-mint text-3xl">🐶</div><h2 className="mt-5 text-xl font-bold">{dog.name}</h2><p className="mt-1 text-sm text-ink/60">{dog.breed}</p><div className="mt-5 flex gap-2"><span className="rounded-full bg-sand px-3 py-1 text-xs font-medium capitalize">{dog.body_size}</span><span className="rounded-full bg-sand px-3 py-1 text-xs font-medium capitalize">{dog.activity_level} activity</span></div></Link>)}</section>}</div></main></AuthGate>; }
