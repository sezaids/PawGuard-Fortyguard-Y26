"use client";
import { useParams, useRouter } from "next/navigation"; import { useEffect, useState } from "react"; import { AuthGate } from "../../../../components/auth-gate"; import { DogForm } from "../../../../components/dog-form"; import { Sidebar } from "../../../../components/sidebar"; import { Dog, request } from "../../../../lib/api";
export default function EditDogPage() { const { id } = useParams<{id:string}>(); const router=useRouter(); const [dog,setDog]=useState<Dog|null>(null); useEffect(()=>{request<Dog>(`/dogs/${id}`).then(setDog).catch(()=>router.replace("/dogs"));},[id,router]); return <AuthGate><main className="min-h-screen bg-sand md:pl-72"><Sidebar/><div className="mx-auto max-w-3xl px-5 pb-12 pt-20 sm:px-8 md:pt-10"><p className="text-sm font-semibold text-moss">MY DOGS / EDIT PROFILE</p><h1 className="mt-1 mb-7 text-3xl font-bold">{dog ? `Update ${dog.name}` : "Loading profile…"}</h1>{dog && <DogForm dog={dog}/>}</div></main></AuthGate>; }
